# Assets

No site was crawled — the subject is a localhost application behind an agent,
so there is no public URL to capture.

## Supplied

- `assets/placeholder-run.mp4` — a 3:17 screen recording of an earlier build.
  **Placeholder only.** It predates the current UI: 10px labels, the 420px
  split, no outcome panel, no rail picker, and a FluxA mandate reading
  "Fund a virtual card…" rather than "Pay <amount> for flight booking <PNR>".
  Every shot cut from it is marked `PLACEHOLDER` in the storyboard and is to be
  replaced from a fresh capture (`demo/CAPTURE.md`).

## Brand

Taken from the app's own stylesheet (`web/app/globals.css`), dark theme:
canvas `#16150f`, surface `#1e1d17`, accent `#6fc3a4`, warn `#d9a94f`,
ink `#efece4`, muted `#a29d90`. Display face Inter; mono `ui-monospace`.
